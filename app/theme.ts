"use client";
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#3157A7",
    },
    secondary: {
      main: "#dc004e",
    },
    grey: {
      50: "#95A0BC",
      100: "#f0f0f0",
    },
  },
});

export default theme;
